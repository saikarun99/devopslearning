#redirectors are of two types
# Input redirection  (<) and output redirection (>) (>>) append the content to the latest existing content

# 1> redirectors the output to another file
# 2> redirects the error to another file.
# &> redirects both error and output to another file.

